name: wdk_safe_relay_kit_flutter
description: Safe smart accounts with ERC-4337 and relay (bundler, paymaster). Port of @tetherto/wdk-safe-relay-kit for Flutter.
version: 1.0.0-beta.1
repository: https://github.com/Jainakin/wdk_safe_relay_kit_flutter

environment:
  sdk: ^3.5.0

dependencies:
  http: ^1.2.0
  meta: ^1.15.0
  pointycastle: ^3.9.1
  web3dart: ^2.7.3

dev_dependencies:
  test: ^1.25.0
  very_good_analysis: ^6.0.0
